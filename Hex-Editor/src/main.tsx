import React from "react";
import ReactDOM from "react-dom/client";
import HexEditor from "./App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <HexEditor />
  </React.StrictMode>,
);
